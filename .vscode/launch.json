{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run example (use integrated terminal)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/examples/ros2_subscribe_and_emit.py",
      "console": "integratedTerminal",
      "justMyCode": true,
      "env": { "PYTHONPATH": "${workspaceFolder}/src" }
    }
  ]
}
